public class ArraysAndStrings {

    ArraysAndStrings() {
        /* TODO: Find a more meaningful constructor */
        System.out.println("Arrays and Strings object instantiated");
    }

    public boolean isPermutation (String a, String b) {
        if (a.length() != b.length()) {
            return false;
        }

        return true;
    }
}